Used Selenium webdriver for WEB Automation project and has below artifacts:
DriverFactory class which initialises the drivers for browser based on parameter passed "Chrome", �E, etc

Tests class in TestNg :
Due to time constraint, was able to automate REST API project full but this one is done till navigation from Parent page and Login till focus on Destination field.

You can import as a Maven project using pom.xml and run as maven project.